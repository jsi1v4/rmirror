[package]
name = "rmirror"
version = "0.1.0"
authors = ["jsi1v4 <git@jsi1v4.com>"]
edition = "2018"

[dependencies]
tokio = { version = "1", features = ["full"] }
hyper = { version = "0.14", features = ["full"] }
hyper-tls = "0.5.0"
